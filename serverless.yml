service: asyncapi
frameworkVersion: '1'

provider:
  name: aws
  runtime: nodejs12.x

functions:
  hello:
    handler: handlers/push.get
    timeout: 30
    events:
      - http:
          path: hello
          method: get
