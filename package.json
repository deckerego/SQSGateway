{
  "name": "SQSGateway",
  "version": "1.0.0",
  "description": "A simple API Gateway that stores inbound API calls for asynchronous remote retrieval",
  "license": "MPL-2.0",
  "private": false,
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.616",
    "@aws-sdk/client-sqs": "^3.616",
    "uuid": "^8.3.2"
  },
  "jest": {
    "verbose": false,
    "collectCoverage": false,
    "testEnvironment": "node",
    "reporters": [
      "default"
    ],
    "testPathIgnorePatterns": [
      "/node_modules/",
      "__tests__/helpers/",
      "/.aws-sam/"
    ]
  }
}
